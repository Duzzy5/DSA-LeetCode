def move_zeros(array):
    j = 0 
    
    for i in range(len(array)):
        if array[i] != 0:
            array[j] = array[i]
            j += 1

    while j < len(array):
        array[j] = 0
        j += 1

    return array


# Example
array = [1, 0, 2, 3, 0, 4, 0, 1]
print(move_zeros(array))
