def highest_occurring_element(arr):
    freq = {}
    for num in arr:
        freq[num] = freq.get(num, 0) + 1
    return max(freq, key=freq.get)


arr = [1, 3, 2, 1, 4, 1, 3, 3, 3]
print(highest_occurring_element(arr))
